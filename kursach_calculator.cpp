#include<iostream>
#include"calculator.h"

using namespace std;


int main()
{
        calculator calc;
        string str;
        str = "log(p)";
        calc.read_string(str);
    return 0;
}
